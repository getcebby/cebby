---
export const prerender = false;

import Layout from '../../layouts/Layout.astro';
import { supabase } from '../../lib/supabase';
import SaveEventButton from '../../components/SaveEventButton.astro';
import EventCard from '../../components/EventCard.astro';
import EventMap from '../../components/EventMap.astro';
import { resolveVenue } from '../../data/venues';
import RsvpButton from '../../components/RsvpButton.astro';
import RSVPModalSimple from '../../components/RSVPModalSimple.astro';
import SmartRSVPButton from '../../components/SmartRSVPButton.astro';
import SEO from '../../components/SEO.astro';
import { getEventTimeDisplay } from '../../utils/date';
import { addUtmParameters } from '../../utils/utm';
import { Image } from 'astro:assets';

const { slug } = Astro.params;
const isNumericId = /^\d+$/.test(slug!);

let event;
let error;

if (isNumericId) {
    const result = await supabase.from('events').select('*,accounts!left(*)').eq('id', parseInt(slug!, 10)).single();
    event = result.data;
    error = result.error;
} else {
    const slugResult = await supabase
        .from('events')
        .select('*,event_slugs!inner(),accounts!left(*)')
        .eq('event_slugs.slug', slug!)
        .single();

    if (slugResult.error || !slugResult.data) {
        const eventId = slug?.split('--')[1];
        const idResult = await supabase
            .from('events')
            .select('*,accounts!left(*)')
            .eq('id', parseInt(eventId!, 10))
            .single();
        event = idResult.data;
        error = idResult.error;
    } else {
        event = slugResult.data;
        error = slugResult.error;
    }
}

if (error) console.error('Error fetching event:', error);
if (!event) return new Response(null, { status: 404 });
if (event?.slug && event?.slug !== slug) return Astro.redirect(`/events/${event.slug}`);

const organizer = event?.accounts;
const formattedDate = event.start_time ? getEventTimeDisplay(event.start_time, event.end_time || undefined) : '';
const ogImage = event?.cover_photo || '/og.png';

const now = new Date();
const startTime = new Date(event.start_time);
const endTime = event.end_time ? new Date(event.end_time) : null;
const hasStarted = now > startTime;
const hasEnded = endTime ? now > endTime : hasStarted && now > new Date(startTime.getTime() + 4 * 60 * 60 * 1000);
const deadlinePassed = event.registration_deadline && now > new Date(event.registration_deadline);
const registrationEnabled = event.registration_enabled;
const registrationClosed = registrationEnabled && (hasStarted || deadlinePassed);

// View-transition morphs only when the event hasn't ended yet. Past events
// don't morph (mirrors EventCard.astro's variant-driven rule).
// Detail page hero is the user's current focus — always activate (no
// viewport gating). The cover image is tall enough (full-width 16:9 on
// mobile) that the title/date/time/location section often sits below
// the fold, so gating would suppress exactly the named elements that
// list-page cards need to morph back to. Inline `style` bypasses the
// global Layout.astro observer.
const vt = !hasEnded
    ? (slot: string) => `view-transition-name: event-${slot}-${event.id}`
    : (_slot: string) => undefined;

const day = startTime.getDate().toString().padStart(2, '0');
const month = startTime.toLocaleString('default', { month: 'short' });

const { data: relatedEvents } = await supabase
    .from('events')
    .select('*')
    .neq('id', event?.id)
    .is('is_hidden', false)
    .gte('start_time', new Date().toISOString())
    .limit(3)
    .order('start_time', { ascending: true });

let rsvpCount = 0;
if (registrationEnabled) {
    const { count } = await supabase
        .from('event_registrations')
        .select('*', { count: 'exact', head: true })
        .eq('event_id', event.id)
        .in('status', ['confirmed', 'pending', 'walkin']);
    if (count) rsvpCount = count;
}

const ctaHeadline = hasEnded
    ? 'Event has ended'
    : registrationClosed
      ? 'Registration closed'
      : registrationEnabled
        ? 'Registration is open'
        : 'Going to this event?';

let ctaSubline: string;
if (hasEnded) {
    ctaSubline = 'Catch the next one — subscribe to the calendar so you do not miss it.';
} else if (rsvpCount === 0) {
    ctaSubline = 'Be the first to register.';
} else if (rsvpCount <= 10) {
    ctaSubline = 'A few people are attending.';
} else {
    ctaSubline = `${rsvpCount} people are attending.`;
}

// Source deep-link — for ended events, "billboard not venue": send curious users
// to the original event page on the source platform so they can see photos,
// recap, attendees, etc.
const facebookEventUrl = `https://www.facebook.com/events/${event.source_id}`;
const baseSourceUrl = event.ticket_url || facebookEventUrl;
const sourceUrl = addUtmParameters(baseSourceUrl, {
    medium: 'ended_event_view',
    eventState: 'ended',
});
const rawSource = (event.source || 'Facebook').toString();
const sourceName = rawSource.charAt(0).toUpperCase() + rawSource.slice(1).toLowerCase();

function linkifyText(text: string): string {
    const urlRegex = /(https?:\/\/[^\s]+)/g;
    return text.replace(urlRegex, (url) => {
        const cleanUrl = url.replace(/[.,;!?]$/, '');
        return `<a href="${cleanUrl}" target="_blank" rel="noopener noreferrer" class="text-purple-700 hover:text-purple-900 underline break-all">${cleanUrl}</a>`;
    });
}

const eventJsonLd = {
    '@context': 'https://schema.org',
    '@type': 'Event',
    name: event.name,
    description: event.description,
    startDate: event.start_time,
    endDate: event.end_time || event.start_time,
    image: event.cover_photo ? new URL(event.cover_photo, Astro.url) : new URL('/og.png', Astro.url),
    location: {
        '@type': 'Place',
        name: event.location,
        address: { '@type': 'PostalAddress', addressLocality: event.location, addressRegion: 'Cebu', addressCountry: 'PH' },
    },
    organizer: { '@type': 'Organization', name: organizer?.name || 'cebby', url: Astro.url.origin },
    offers: { '@type': 'Offer', price: '0', priceCurrency: 'PHP', availability: 'https://schema.org/InStock', url: Astro.url.href, validFrom: event.created_at },
    eventStatus: 'https://schema.org/ScheduledEvent',
    eventAttendanceMode: event.location?.toLowerCase()?.includes('online')
        ? 'https://schema.org/OnlineEventAttendanceMode'
        : 'https://schema.org/OfflineEventAttendanceMode',
    performer: { '@type': 'Organization', name: organizer?.name || 'Event Organizer' },
};
---

<Layout title={`${event.name} - cebby`}>
    <Fragment slot="head">
        <SEO title={`${event.name} - cebby`} description={event?.description || 'Cebu tech events'} image={ogImage} />
        <script is:inline type="application/ld+json" set:html={JSON.stringify(eventJsonLd)} />
    </Fragment>

    <main class="mx-auto max-w-2xl px-5 pb-16 pt-safe">
        {/* Top bar — back button + share/save */}
        <header class="flex items-center justify-between pt-6 pb-5">
            <a
                id="backButton"
                href="/events"
                class="press-sm group chrome-in-left shadow-soft-1 flex h-10 w-10 items-center justify-center rounded-full bg-white text-gray-700 transition-colors duration-200 hover:text-purple-700"
                style="animation-delay: 360ms"
                aria-label="Back to events"
            >
                <svg width="18" height="18" viewBox="0 0 18 18" fill="none" class="transition-transform duration-200 group-hover:-translate-x-0.5 motion-reduce:transition-none motion-reduce:group-hover:translate-x-0">
                    <path d="M11 4l-5 5 5 5" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" />
                </svg>
            </a>
            <div class="flex items-center gap-2">
                <div class="relative chrome-in-right" style="animation-delay: 420ms">
                    <button
                        id="shareButton"
                        class="press-sm shadow-soft-1 flex h-10 items-center gap-2 rounded-full bg-white px-4 text-sm font-semibold text-gray-700 transition-all duration-200 hover:-translate-y-0.5 hover:text-purple-700 aria-expanded:bg-purple-50 aria-expanded:text-purple-700 motion-reduce:transition-none motion-reduce:hover:translate-y-0"
                        aria-label="Share event"
                        aria-haspopup="menu"
                        aria-expanded="false"
                    >
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <circle cx="18" cy="5" r="3" />
                            <circle cx="6" cy="12" r="3" />
                            <circle cx="18" cy="19" r="3" />
                            <line x1="8.59" y1="13.51" x2="15.42" y2="17.49" />
                            <line x1="15.41" y1="6.51" x2="8.59" y2="10.49" />
                        </svg>
                        Share
                    </button>
                    <div id="shareMenu" class="absolute right-0 top-full z-50 mt-2 hidden" role="menu">
                        <div class="card-tactile w-56 overflow-hidden p-1">
                            <button id="copyLinkButton" class="press-sm flex w-full items-center gap-3 rounded-xl px-4 py-3 text-left text-sm font-medium text-gray-700 hover:bg-gray-50">
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                                    <path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71" />
                                    <path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71" />
                                </svg>
                                <span data-label>Copy link</span>
                            </button>
                            <button id="nativeShareButton" class="press-sm flex w-full items-center gap-3 rounded-xl px-4 py-3 text-left text-sm font-medium text-gray-700 hover:bg-gray-50">
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                                    <circle cx="18" cy="5" r="3" />
                                    <circle cx="6" cy="12" r="3" />
                                    <circle cx="18" cy="19" r="3" />
                                    <line x1="8.59" y1="13.51" x2="15.42" y2="17.49" />
                                    <line x1="15.41" y1="6.51" x2="8.59" y2="10.49" />
                                </svg>
                                <span>More options</span>
                            </button>
                        </div>
                    </div>
                </div>
                <div class="chrome-in-right" style="animation-delay: 480ms">
                    <SaveEventButton event={event} />
                </div>
            </div>
        </header>

        {/* Cover photo */}
        {
            event.cover_photo && (
                <div class="lift-hover card-tactile mb-6 overflow-hidden">
                    <button
                        type="button"
                        id="expandImageBtn"
                        class="press block w-full"
                        aria-label="View full image"
                    >
                        <Image
                            src={event.cover_photo}
                            alt={event.name}
                            width={1200}
                            height={675}
                            loading="eager"
                            class="cover-zoom h-auto w-full"
                            style={vt('cover')}
                        />
                    </button>
                </div>
            )
        }

        {/* Title + meta card */}
        <section class="card-tactile mb-6 p-6">
            <div class="mb-4 flex items-start gap-4">
                <div
                    class={`date-chip ${event.cover_photo ? '' : 'date-chip-purple'} flex-shrink-0 text-center`}
                    style={vt('date')}
                >
                    <div class={`text-xl font-extrabold leading-none ${event.cover_photo ? 'text-gray-900' : 'text-purple-900'}`}>
                        {day}
                    </div>
                    <div class={`mt-1 text-[10px] font-bold uppercase tracking-wider ${event.cover_photo ? 'text-gray-500' : 'text-purple-700'}`}>
                        {month}
                    </div>
                </div>
                <div class="min-w-0 flex-1">
                    <h1
                        class="mb-1 text-xl font-extrabold leading-snug text-gray-900"
                        data-organization={organizer?.name}
                        style={vt('title')}
                    >
                        {event.name}
                    </h1>
                    {organizer?.name && <p class="text-sm font-medium text-gray-500">by {organizer.name}</p>}
                </div>
            </div>

            <div class="space-y-2 text-sm text-gray-700">
                <div class="flex items-center gap-2">
                    <svg class="h-4 w-4 flex-shrink-0 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.75" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <time datetime={event.start_time} style={vt('time')}>
                        {formattedDate}
                    </time>
                    {event.end_time && <time class="hidden" datetime={event.end_time} data-end-time></time>}
                </div>
                {event.location && (
                    <div class="flex items-start gap-2">
                        <svg class="mt-0.5 h-4 w-4 flex-shrink-0 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.75" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.75" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                        </svg>
                        <span data-location style={vt('location')}>
                            {event.location}
                        </span>
                    </div>
                )}
            </div>
        </section>

        {/* Primary CTA — registration */}
        <section class="detail-section-in card-tactile mb-6 p-5" style="animation-delay: 360ms">
            <div class="mb-1 text-base font-extrabold text-gray-900">{ctaHeadline}</div>
            <p class="mb-4 text-sm text-gray-500">{ctaSubline}</p>
            <div class="flex flex-wrap items-center gap-3">
                {
                    !hasEnded && registrationEnabled ? (
                        <>
                            <SmartRSVPButton
                                eventId={event.id?.toString()}
                                eventName={event?.name || ''}
                                registrationLimit={event.registration_limit}
                                registrationDeadline={event.registration_deadline}
                                eventStartTime={event.start_time}
                            />
                            <RSVPModalSimple eventId={event.id?.toString()} eventName={event?.name || ''} isManagedEvent={true} />
                        </>
                    ) : !hasEnded ? (
                        <RsvpButton event={event} />
                    ) : (
                        <>
                            <a href="/calendar" class="press-sm btn-primary-tactile">
                                Subscribe to calendar →
                            </a>
                            <a
                                href={sourceUrl}
                                target="_blank"
                                rel="noopener noreferrer"
                                class="press-sm shadow-soft-1 flex h-10 items-center gap-2 rounded-full bg-white px-4 text-sm font-semibold text-gray-700 transition-all duration-200 hover:-translate-y-0.5 hover:text-purple-700 motion-reduce:transition-none motion-reduce:hover:translate-y-0"
                            >
                                View on {sourceName}
                                <svg
                                    width="14"
                                    height="14"
                                    viewBox="0 0 24 24"
                                    fill="none"
                                    stroke="currentColor"
                                    stroke-width="2"
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    aria-hidden="true"
                                >
                                    <path d="M15 3h6v6" />
                                    <path d="M10 14 21 3" />
                                    <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
                                </svg>
                            </a>
                        </>
                    )
                }
                {
                    !hasEnded && (
                        <button
                            id="addToCalendarButton"
                            class="press-sm shadow-soft-1 flex h-10 items-center gap-2 rounded-full bg-white px-4 text-sm font-semibold text-gray-700 transition-all duration-200 hover:-translate-y-0.5 hover:text-purple-700 motion-reduce:transition-none motion-reduce:hover:translate-y-0"
                        >
                            <svg
                                width="16"
                                height="16"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="1.75"
                                stroke-linecap="round"
                            >
                                <path d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                            </svg>
                            Add to calendar
                        </button>
                    )
                }
            </div>
        </section>

        {/* Map */}
        {
            event.location && (
                <section class="detail-section-in mb-6" style="animation-delay: 460ms">
                    <h2 class="mb-3 text-lg font-bold text-gray-900">Location</h2>
                    <div class="card-tactile overflow-hidden">
                        <EventMap location={event.location} name={event.name || ''} />
                    </div>
                    {(() => {
                        const venue = resolveVenue(event.location);
                        return venue ? (
                            <a
                                href={`/venues/${venue.slug}`}
                                class="press-sm arrow-nudge mt-3 inline-flex items-center gap-1.5 text-sm font-semibold text-purple-700 hover:text-purple-900"
                            >
                                More at {venue.name}
                                <span aria-hidden="true">→</span>
                            </a>
                        ) : null;
                    })()}
                </section>
            )
        }

        {/* Description */}
        {
            event.description && (
                <section class="detail-section-in mb-6" style="animation-delay: 560ms">
                    <h2 class="mb-3 text-lg font-bold text-gray-900">About</h2>
                    <div class="card-tactile p-6">
                        <p
                            class="whitespace-pre-wrap text-sm leading-relaxed text-gray-700"
                            set:html={linkifyText(event.description)}
                        />
                    </div>
                </section>
            )
        }

        {/* Related events */}
        {
            relatedEvents && relatedEvents.length > 0 && (
                <section class="detail-section-in mb-6" style="animation-delay: 660ms">
                    <h2 class="mb-3 text-lg font-bold text-gray-900">Similar events</h2>
                    <div class="grid gap-4 sm:grid-cols-2">
                        {relatedEvents.map((e) => (
                            <EventCard event={e as any} />
                        ))}
                    </div>
                </section>
            )
        }

        <p class="mt-8 text-center text-sm text-gray-500">
            Spotted a problem with this event?{' '}
            <button
                type="button"
                data-open-feedback
                class="font-semibold text-gray-700 underline-offset-4 transition-colors hover:text-purple-700 hover:underline"
            >
                Send feedback
            </button>
        </p>
    </main>

    {/* Lightbox */}
    {
        event.cover_photo && (
            <div id="imageModal" class="lightbox fixed inset-0 z-[9999] flex items-center justify-center bg-black/90 opacity-0 pointer-events-none transition-opacity duration-200">
                <button type="button" id="closeModalBtn" class="press-sm absolute right-5 top-5 flex h-10 w-10 items-center justify-center rounded-full bg-white/10 text-white transition-all duration-200 hover:scale-110 hover:bg-white/20 motion-reduce:transition-none motion-reduce:hover:scale-100" aria-label="Close">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
                        <line x1="18" y1="6" x2="6" y2="18" />
                        <line x1="6" y1="6" x2="18" y2="18" />
                    </svg>
                </button>
                <Image src={event.cover_photo} alt={event.name} width={1920} height={1080} class="max-h-[90vh] max-w-[95%] object-contain" />
            </div>
        )
    }

</Layout>

<script>
    if (typeof window !== 'undefined' && (window as any).umami) {
        (window as any).umami.track('event_view', {
            event_id: window.location.pathname.split('/').pop(),
            event_name: document.title,
            organization: document.querySelector('[data-organization]')?.getAttribute('data-organization'),
        });
    }

    // Back button — use history.back() when arriving from an internal page so
    // bfcache restores the previous scroll position. Falls back to /events
    // (the href) for direct hits, external referrers, or first-page-in-tab.
    const backBtn = document.getElementById('backButton');
    if (backBtn) {
        backBtn.addEventListener('click', (e) => {
            const sameOriginReferrer =
                document.referrer && new URL(document.referrer).origin === window.location.origin;
            if (sameOriginReferrer && window.history.length > 1) {
                e.preventDefault();
                window.history.back();
            }
        });
    }

    // Lightbox
    const expandBtn = document.getElementById('expandImageBtn');
    const modal = document.getElementById('imageModal');
    const closeBtn = document.getElementById('closeModalBtn');
    if (expandBtn && modal && closeBtn) {
        const openModal = () => {
            modal.classList.remove('opacity-0', 'pointer-events-none');
            modal.classList.add('opacity-100');
            document.body.style.overflow = 'hidden';
        };
        const closeModal = () => {
            modal.classList.remove('opacity-100');
            modal.classList.add('opacity-0', 'pointer-events-none');
            document.body.style.overflow = '';
        };
        expandBtn.addEventListener('click', openModal);
        closeBtn.addEventListener('click', closeModal);
        modal.addEventListener('click', (e) => {
            if (e.target === modal) closeModal();
        });
    }

    // Share menu
    const shareBtn = document.getElementById('shareButton');
    const shareMenu = document.getElementById('shareMenu');
    const copyLinkBtn = document.getElementById('copyLinkButton');
    const nativeShareBtn = document.getElementById('nativeShareButton');

    if (shareBtn && shareMenu) {
        shareBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            const open = shareMenu.classList.toggle('hidden') === false;
            shareBtn.setAttribute('aria-expanded', String(open));
        });
        document.addEventListener('click', (e) => {
            if (!shareMenu.contains(e.target as Node) && e.target !== shareBtn) {
                shareMenu.classList.add('hidden');
                shareBtn.setAttribute('aria-expanded', 'false');
            }
        });
    }

    if (copyLinkBtn) {
        const successClasses = ['text-emerald-700', 'bg-emerald-50'];
        const baseClasses = ['text-gray-700'];
        copyLinkBtn.addEventListener('click', async () => {
            const labelEl = copyLinkBtn.querySelector('[data-label]') as HTMLElement | null;
            const iconEl = copyLinkBtn.querySelector('svg');
            try {
                await navigator.clipboard.writeText(window.location.href);
                if (labelEl) labelEl.textContent = 'Copied!';
                copyLinkBtn.classList.remove(...baseClasses);
                copyLinkBtn.classList.add(...successClasses);
                if (iconEl) {
                    iconEl.classList.remove('pop-once');
                    void (iconEl as SVGElement).getBoundingClientRect();
                    iconEl.classList.add('pop-once');
                }
                setTimeout(() => {
                    if (labelEl) labelEl.textContent = 'Copy link';
                    copyLinkBtn.classList.remove(...successClasses);
                    copyLinkBtn.classList.add(...baseClasses);
                    iconEl?.classList.remove('pop-once');
                }, 1500);
            } catch (err) {
                console.error('Failed to copy:', err);
            }
        });
    }

    if (nativeShareBtn) {
        nativeShareBtn.addEventListener('click', async () => {
            if (navigator.share) {
                try {
                    await navigator.share({ title: document.title, url: window.location.href });
                } catch (err) {
                    if ((err as Error).name !== 'AbortError') console.error(err);
                }
            }
            shareMenu?.classList.add('hidden');
        });
    }

    // Add to calendar
    const addToCalendarBtn = document.getElementById('addToCalendarButton');
    if (addToCalendarBtn) {
        addToCalendarBtn.addEventListener('click', () => {
            const startTimeEl = document.querySelector('time[datetime]') as HTMLTimeElement | null;
            const endTimeEl = document.querySelector('[data-end-time]') as HTMLTimeElement | null;
            const locationEl = document.querySelector('[data-location]') as HTMLElement | null;

            const title = document.title.replace(' - cebby', '');
            const start = startTimeEl?.dateTime || '';
            const end = endTimeEl?.dateTime || start;
            const location = locationEl?.textContent?.trim() || '';

            const formatDate = (iso: string) => iso.replace(/[-:]/g, '').replace(/\.\d+/, '');
            const url = `https://calendar.google.com/calendar/render?action=TEMPLATE&text=${encodeURIComponent(title)}&dates=${formatDate(start)}/${formatDate(end)}&details=${encodeURIComponent(window.location.href)}&location=${encodeURIComponent(location)}`;
            window.open(url, '_blank');
        });
    }
</script>

<style>
    /* Top-bar chrome entrance — runs once when the detail page loads.
       User-triggered (the tap that opened this event), so it respects
       principle #5 (tactility ON, ambient motion OFF). */
    @keyframes chrome-in-left {
        0%   { transform: translateX(-12px) scale(0.85); opacity: 0; }
        100% { transform: translateX(0) scale(1); opacity: 1; }
    }
    @keyframes chrome-in-right {
        0%   { transform: translateX(12px) scale(0.85); opacity: 0; }
        100% { transform: translateX(0) scale(1); opacity: 1; }
    }
    .chrome-in-left {
        animation: chrome-in-left 360ms cubic-bezier(0.34, 1.56, 0.64, 1) backwards;
    }
    .chrome-in-right {
        animation: chrome-in-right 360ms cubic-bezier(0.34, 1.56, 0.64, 1) backwards;
    }

    /* Body sections cascade in after the cover/title/date morph finishes
       (cover morph ~360ms). Staggered via inline animation-delay so each
       section reveals in sequence rather than competing with the morph. */
    @keyframes detail-section-in {
        0%   { transform: translateY(10px); opacity: 0; }
        100% { transform: translateY(0); opacity: 1; }
    }
    .detail-section-in {
        animation: detail-section-in 360ms cubic-bezier(0.2, 0, 0, 1) backwards;
    }

    @media (prefers-reduced-motion: reduce) {
        .chrome-in-left,
        .chrome-in-right,
        .detail-section-in {
            animation: none;
        }
    }
</style>
