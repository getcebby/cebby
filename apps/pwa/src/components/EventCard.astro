---
import type { EventFromDB } from '../types/database';
import { getEventTimeDisplay } from '../utils/date';
import { getOrganizers } from '../lib/event-attribution';
import { Image } from 'astro:assets';
import VenueChip from './VenueChip.astro';

interface Props {
    event: EventFromDB;
    variant?: 'upcoming' | 'recent' | 'past';
}

const { event, variant = 'upcoming' } = Astro.props;

const eventDate = new Date(event.start_time);
const day = eventDate.getDate().toString().padStart(2, '0');
const month = eventDate.toLocaleString('default', { month: 'short' });

const detailPageUrl = `/events/${event.slug || event.id}`;
const timeDisplay = getEventTimeDisplay(event.start_time, event.end_time ?? undefined);
const organizers = getOrganizers(event);
const organizerLabel = organizers.length <= 2
    ? organizers.map((organizer) => organizer.name).join(', ')
    : `${organizers[0].name}, ${organizers[1].name} + ${organizers.length - 2} more`;
// `variant` stays in the API as a hook for future per-context styling, but
// past/recent no longer auto-dim — the section heading already provides
// temporal context, and the dim was making the homepage hero peek look stale.

// Viewport gate in Layout.astro decides whether this morph runs.
const vtName = (slot: string) => `event-${slot}-${event.id}`;
---

<a
    href={detailPageUrl}
    class="press lift-hover card-tactile block overflow-hidden"
    data-astro-prefetch="tap"
>
    {
        event.cover_photo ? (
            <div class="relative aspect-[16/9] overflow-hidden">
                <Image
                    src={event.cover_photo}
                    alt={event.name}
                    width={800}
                    height={450}
                    loading="lazy"
                    class="cover-zoom h-full w-full object-cover"
                    data-vt-name={vtName('cover')}
                />
                <div class="date-chip absolute left-4 top-4 text-center" data-vt-name={vtName('date')}>
                    <div class="text-xl font-extrabold leading-none text-gray-900">{day}</div>
                    <div class="mt-1 text-[10px] font-bold uppercase tracking-wider text-gray-500">
                        {month}
                    </div>
                </div>
            </div>
        ) : null
    }

    <div class="p-5">
        {
            !event.cover_photo && (
                <div class="mb-3 flex items-start justify-between">
                    <div class="date-chip date-chip-purple inline-block text-center" data-vt-name={vtName('date')}>
                        <div class="text-xl font-extrabold leading-none text-purple-900">{day}</div>
                        <div class="mt-1 text-[10px] font-bold uppercase tracking-wider text-purple-700">
                            {month}
                        </div>
                    </div>
                    {event?.account_id && (
                        <span class="pill-tactile pill-emerald">
                            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M20 6 9 17l-5-5" />
                            </svg>
                            Verified
                        </span>
                    )}
                </div>
            )
        }

        <h3
            class="mb-1.5 text-lg font-extrabold leading-snug text-gray-900 line-clamp-2"
            data-vt-name={vtName('title')}
        >
            {event.name}
        </h3>

        <p class="flex flex-wrap items-center gap-x-1.5 text-sm text-gray-500 line-clamp-1">
            {organizerLabel && (
                <>
                    <span class="font-medium text-gray-700">{organizerLabel}</span>
                    <span aria-hidden="true">·</span>
                </>
            )}
            <time datetime={event.start_time} data-vt-name={vtName('time')}>
                {timeDisplay}
            </time>
            {(event.location || event.is_online) && (
                <>
                    <span aria-hidden="true">·</span>
                    <span data-vt-name={vtName('location')}>
                        <VenueChip location={event.location} isOnline={event.is_online} />
                    </span>
                </>
            )}
        </p>
    </div>
</a>

<style>
    .line-clamp-1 {
        display: -webkit-box;
        -webkit-line-clamp: 1;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
    .line-clamp-2 {
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
</style>
