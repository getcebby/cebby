# Copy to apps/admin/.env and fill in. The simplest path: copy the relevant
# values from apps/pwa/.env (same Supabase project, same admin password).

PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
ADMIN_PASSWORD=
